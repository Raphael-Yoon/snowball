# Snowball 테스트 가이드 📚

> 언제든지 이 문서를 읽고 따라하면 테스트를 실행할 수 있습니다!

## 📖 목차

1. [빠른 시작](#빠른-시작)
2. [단위 테스트](#단위-테스트-unit-tests)
3. [E2E 테스트](#e2e-테스트-end-to-end-tests)
4. [언제 어떤 테스트를 실행하나요?](#언제-어떤-테스트를-실행하나요)
5. [문제 해결](#문제-해결)

---

## 빠른 시작

### 1️⃣ 첫 설정 (처음 한 번만)

```bash
# 프로젝트 디렉토리로 이동
cd c:/Python/snowball

# 의존성 설치
pip install -r requirements.txt

# Playwright 브라우저 설치 (E2E 테스트용)
playwright install chromium
```

### 2️⃣ 전체 테스트 실행 (권장) ⭐

```bash
# 단위 테스트 + E2E 테스트를 한 번에 실행하고 결과를 하나의 파일로 통합
python test/run_all_tests.py

# E2E 테스트 실행 시 애플리케이션 실행 여부를 확인
# y를 입력하면 E2E 테스트도 실행, n을 입력하면 단위 테스트만 실행
```

**결과 파일**: `test/test_result_YYYYMMDD_HHMMSS.txt` (통합 결과)

### 3️⃣ 단위 테스트만 실행 (빠름 - 1~2분)

```bash
python test/test_units_integrated.py
```

**결과 파일**: `test/unit_test_result_YYYYMMDD_HHMMSS.txt`

### 4️⃣ E2E 테스트만 실행 (느림 - 10~15분)

```bash
# 터미널 1: 애플리케이션 실행
python snowball.py

# 터미널 2: E2E 테스트 실행
python test/test_e2e_integrated.py --headless
```

**결과 파일**: `test/e2e_test_result_YYYYMMDD_HHMMSS.txt`

---

## 단위 테스트 (Unit Tests)

### 🎯 특징

- ⚡ **빠름**: 1~2분 소요
- 🔧 **Mock 기반**: 실제 DB/이메일 발송 없음
- 💻 **브라우저 불필요**: 터미널에서만 실행
- 🎯 **검증 범위**: 함수/API 레벨

### 📝 사용법

#### 전체 모듈 테스트
```bash
python test/test_units_integrated.py
```

#### 특정 모듈만 테스트
```bash
# Auth 모듈만
python test/test_units_integrated.py --module=auth

# Link5 (RCM 관리) 모듈만
python test/test_units_integrated.py --module=link5

# Link6 (설계평가) 모듈만
python test/test_units_integrated.py --module=link6

# Link7 (운영평가) 모듈만
python test/test_units_integrated.py --module=link7
```

### 📊 모듈 목록

| 모듈 | 내용 | 테스트 개수 |
|------|------|------------|
| `auth` | 인증, OTP, 권한, 세션 | 2개 |
| `link1` | RCM 자동생성 | 1개 |
| `link5` | RCM 업로드, 파일 검증 | 2개 |
| `link6` | 설계평가 | 1개 |
| `link7` | 운영평가 | 1개 |
| `all` (기본값) | 위 모든 모듈 | 7개 |

### 📄 결과 저장

테스트 실행 후 결과는 다음 위치에 텍스트 파일로 자동 저장됩니다:
- **위치**: `test/unit_test_result_YYYYMMDD_HHMMSS.txt`
- **내용**: 모듈별 통과율, 전체 요약 통계

### 🎬 실행 결과 예시

```
================================================================================
Snowball 통합 단위 테스트
================================================================================
시작 시간: 2026-01-18 16:30:45
대상 모듈: all

================================================================================
Auth: 인증 및 권한
================================================================================

✅ test_otp_generation (0.01s) - 6자리 숫자 OTP 생성 확인
    ℹ️  생성된 OTP: 123456
✅ test_login_required (0.02s) - login_required 데코레이터 작동 확인

================================================================================
Link5: RCM 관리
================================================================================

✅ test_link5_route (0.01s) - Link5 Blueprint 등록 확인
✅ test_file_validation (0.02s) - 파일 검증 로직이 구현되어 있습니다

================================================================================
테스트 결과 요약
================================================================================

모듈별 결과:
  auth      :   2/  2 통과
  link5     :   2/  2 통과
  link6     :   1/  1 통과
  link7     :   1/  1 통과

총계:
  총 테스트: 6개
  ✅ 통과: 6개 (100.0%)
  ❌ 실패: 0개 (0.0%)
```

---

## E2E 테스트 (End-to-End Tests)

### 🎯 특징

- 🐢 **느림**: 10~15분 소요
- 🌐 **실제 브라우저**: Chromium 사용
- 🎨 **UI/UX 검증**: 실제 사용자처럼 클릭, 입력
- 📸 **스크린샷**: 자동 캡처 (`test/screenshots/`)
- ✅ **전체 플로우**: 로그인부터 완료까지

### ⚠️ 중요: 애플리케이션 실행 필요!

E2E 테스트는 **실행 중인 애플리케이션**을 테스트합니다.

```bash
# 반드시 먼저 실행! (터미널 1)
python snowball.py

# 그 다음 E2E 테스트 (터미널 2)
python test/test_e2e_integrated.py
```

### 📝 사용법

#### 전체 스위트 테스트
```bash
python test/test_e2e_integrated.py
```

#### 특정 스위트만 테스트
```bash
# Auth (인증) 스위트만
python test/test_e2e_integrated.py --suite=auth

# RCM (생성 + 업로드) 스위트만
python test/test_e2e_integrated.py --suite=rcm

# Evaluation (설계평가 + 운영평가) 스위트만
python test/test_e2e_integrated.py --suite=evaluation

# Interview (ITGC 인터뷰) 스위트만
python test/test_e2e_integrated.py --suite=interview
```

#### 헤드리스 모드 (브라우저 UI 숨김 - 추천!)
```bash
# 빠르고 조용하게 실행
python test/test_e2e_integrated.py --headless

# 특정 스위트만 헤드리스로
python test/test_e2e_integrated.py --suite=auth --headless
```

#### 다른 URL 테스트
```bash
# 다른 포트에서 실행 중인 경우
python test/test_e2e_integrated.py --url=http://localhost:8000
```

### 📊 스위트 목록

| 스위트 | 내용 | 실행 시간 | 테스트 개수 |
|--------|------|----------|------------|
| `auth` | 로그인, OTP, 세션 | 2~3분 | 2개 |
| `rcm` | RCM 생성 + 업로드 | 3~5분 | 2개 |
| `evaluation` | 설계평가 + 운영평가 | 4~6분 | 2개 |
| `interview` | ITGC 인터뷰 | 2~3분 | 1개 |
| `all` (기본값) | 위 모든 스위트 | 10~15분 | 7개 |

### 📄 결과 저장 및 정리

테스트 실행 후:
- **결과 저장**: `test/e2e_test_result_YYYYMMDD_HHMMSS.txt`
- **자동 정리**: 테스트 과정에서 생성된 스크린샷, JSON 파일은 자동 삭제됨
- **결과만 유지**: 텍스트 결과 파일만 test 폴더에 남음

### 🎬 실행 결과 예시

```
================================================================================
Snowball 통합 E2E 테스트 (Playwright)
================================================================================
시작 시간: 2026-01-18 16:35:12
Base URL: http://localhost:5000
Headless: True
대상 스위트: auth

⚠️  주의: E2E 테스트를 실행하기 전에 애플리케이션이 실행 중이어야 합니다!
   기본 URL: http://localhost:5000

================================================================================
Auth: 인증 플로우
================================================================================

✅ test_login_page_loads (2.15s) - 로그인 페이지 로드 성공
✅ test_complete_login_flow (3.45s) - 로그인 플로우 완료

================================================================================
E2E 테스트 결과 요약
================================================================================

총 테스트: 2개
✅ 통과: 2개 (100.0%)
❌ 실패: 0개 (0.0%)
⚠️ 경고: 0개 (0.0%)
⊘ 건너뜀: 0개 (0.0%)

📷 스크린샷: test/screenshots/ (4개)
```

### 📸 스크린샷 확인

테스트 실패 시 자동으로 스크린샷이 저장됩니다:

```bash
# 스크린샷 디렉토리 확인
ls test/screenshots/

# 예시 파일들
# login_page_20260118_163512.png
# error_test_login_flow_20260118_163520.png
```

---

## 언제 어떤 테스트를 실행하나요?

### 🔄 개발 워크플로우

#### 1. 개발 중 (코드 수정 후)
```bash
# 빠른 검증: 수정한 모듈만 단위 테스트
python test/test_units_integrated.py --module=link5
```
**시간**: 10초~1분

---

#### 2. 커밋 전
```bash
# 전체 단위 테스트
python test/test_units_integrated.py
```
**시간**: 1~2분

---

#### 3. PR (Pull Request) 생성 시
```bash
# 단위 테스트 먼저
python test/test_units_integrated.py

# 핵심 E2E 테스트
python snowball.py &
sleep 5
python test/test_e2e_integrated.py --suite=rcm --headless
python test/test_e2e_integrated.py --suite=evaluation --headless
```
**시간**: 10~15분

---

#### 4. 배포 전 (최종 검증)
```bash
# 1. 단위 테스트 전체
python test/test_units_integrated.py

# 2. E2E 테스트 전체
python snowball.py &
sleep 5
python test/test_e2e_integrated.py --headless
```
**시간**: 15~20분

---

### 📋 빠른 참조표

| 상황 | 실행 명령 | 시간 |
|------|----------|------|
| 코드 수정 후 | `python test/test_units_integrated.py --module=link5` | 10초 |
| 커밋 전 | `python test/test_units_integrated.py` | 1분 |
| PR 생성 시 | `python test/test_e2e_integrated.py --suite=rcm --headless` | 5분 |
| 배포 전 | `python test/test_e2e_integrated.py --headless` | 15분 |

---

## 문제 해결

### ❌ "playwright: command not found"

**해결책**:
```bash
pip install playwright
playwright install chromium
```

---

### ❌ "Connection refused" (E2E 테스트)

**원인**: 애플리케이션이 실행되지 않음

**해결책**:
```bash
# 터미널 1에서 먼저 실행
python snowball.py

# 브라우저에서 확인
# http://localhost:5000 접속 확인

# 그 다음 터미널 2에서 테스트
python test/test_e2e_integrated.py
```

---

### ❌ "Element not found" (E2E 테스트)

**원인**: 페이지 로딩이 느리거나 UI가 변경됨

**해결책**:
```bash
# 1. 헤드리스 모드 끄고 직접 확인
python test/test_e2e_integrated.py  # 브라우저 UI 보임

# 2. 스크린샷 확인
ls test/screenshots/
```

---

### ❌ "Import Error"

**원인**: 의존성 미설치

**해결책**:
```bash
pip install -r requirements.txt
```

---

### ⚠️ 테스트가 너무 느림

**해결책**:
```bash
# 1. 특정 모듈/스위트만 실행
python test/test_units_integrated.py --module=auth
python test/test_e2e_integrated.py --suite=auth

# 2. 헤드리스 모드 사용
python test/test_e2e_integrated.py --headless
```

---

### ⚠️ 포트 충돌 (5000번 포트 사용 중)

**확인**:
```bash
# Windows
netstat -ano | findstr :5000

# Linux/Mac
lsof -i :5000
```

**해결책**:
```bash
# 다른 포트로 실행
python snowball.py --port=8000

# E2E 테스트도 URL 변경
python test/test_e2e_integrated.py --url=http://localhost:8000
```

---

## 🎓 추가 학습 자료

### 상세 문서

| 문서 | 위치 | 내용 |
|------|------|------|
| 테스트 메인 가이드 | `test/README.md` | 통합 테스트 상세 설명 |
| Playwright 가이드 | `test/PLAYWRIGHT_GUIDE.md` | E2E 테스트 작성법 |
| 빠른 시작 | `test/QUICKSTART_E2E.md` | 5분 튜토리얼 |
| 시나리오 목록 | `test/E2E_TEST_SCENARIOS.md` | 전체 테스트 시나리오 |

### 파일 위치

```
snowball/
├── TESTING_GUIDE.md              # 이 파일 ⭐
│
├── test/
│   ├── test_units_integrated.py  # 단위 테스트
│   ├── test_e2e_integrated.py    # E2E 테스트
│   ├── playwright_base.py        # E2E 유틸리티
│   │
│   └── legacy_backup/            # 기존 파일 백업 (21개)
│       ├── auth_test.py
│       ├── link1_test.py
│       └── ...
│
└── requirements.txt              # 의존성 목록
```

---

## 🚀 한 번에 복사해서 사용하기

### 개발 중 (빠른 검증)
```bash
python test/test_units_integrated.py --module=link5
```

### 커밋 전
```bash
python test/test_units_integrated.py
```

### PR 생성 시
```bash
python test/test_units_integrated.py && \
(python snowball.py &) && \
sleep 5 && \
python test/test_e2e_integrated.py --suite=rcm --headless
```

### 배포 전 (전체 검증)
```bash
python test/test_units_integrated.py && \
(python snowball.py &) && \
sleep 5 && \
python test/test_e2e_integrated.py --headless
```

---

## 💡 팁

### 1. 브라우저 UI 보면서 디버깅
```bash
# --headless 옵션 제거
python test/test_e2e_integrated.py --suite=auth
```

### 2. 특정 테스트만 빠르게
```bash
# Auth만
python test/test_units_integrated.py --module=auth
python test/test_e2e_integrated.py --suite=auth --headless
```

### 3. 스크린샷으로 문제 파악
```bash
# 테스트 실패 시 자동 생성됨
ls test/screenshots/error_*.png
```

### 4. 백그라운드 실행
```bash
# Linux/Mac
python snowball.py &

# Windows (별도 터미널 필요)
start python snowball.py
```

---

## 📞 도움이 필요하면?

1. **문서 확인**: `test/README.md` 읽기
2. **스크린샷 확인**: `test/screenshots/` 폴더
3. **이슈 보고**: GitHub Issues

---

**버전**: 2.0
**작성일**: 2026-01-18
**문서 위치**: `snowball/TESTING_GUIDE.md`

**이 파일을 언제든지 읽고 따라하세요!** 📚✨
